# pnpm 10+ 不再读 package.json 的 pnpm 字段，改读此文件。
# electron / esbuild 需跑 postinstall（下载/编译 native 二进制），必须显式批准。
# 本地与 CI 统一使用 pnpm 11，仅此文件作为 onlyBuiltDependencies 的唯一来源。
onlyBuiltDependencies:
  - electron
  - esbuild
